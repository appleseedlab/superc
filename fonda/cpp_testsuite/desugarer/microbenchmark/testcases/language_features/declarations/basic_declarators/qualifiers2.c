#ifdef DUPLICATE
const const float a;
const const const const const float b;
volatile volatile volatile volatile int c;
#endif

#ifdef LONG
long d;
long long e;
long int f;
long long int g;
long double h;
#endif
